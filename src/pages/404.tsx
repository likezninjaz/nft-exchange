import { Typography } from 'components';

const Page404 = () => (
  <Typography
    typographyStyle={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      width: '100%',
      height: '100%',
    }}
  >
    Oops, page not found
  </Typography>
);

export default Page404;
